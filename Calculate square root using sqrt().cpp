#include <iostream>
#include <cmath>
using namespace std;

float squareRoot(float);

int main() {
    cout << squareRoot(64);
    return 0;
}

float squareRoot(float x) {
    return sqrt(x);
}